.fireflies {
    position:fixed;
    inset:0;
    pointer-events:none;
    color:#eac67a;
    font-size:20px;
    opacity:.4;
    animation:drift 18s linear infinite;
}

@keyframes drift {
    from{
        transform:translateY(100vh);
    }
    to{
        transform:translateY(-100vh);
    }
}
